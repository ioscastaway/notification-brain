<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws everything; this only exists so the window has a sane theme before Compose attaches. -->
    <style name="Theme.NotificationBrain" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
